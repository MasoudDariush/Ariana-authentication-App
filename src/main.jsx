import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App.jsx";
import './index.css'
import { Provider } from "react-redux";
import { store } from "./store/store.js";
import "./api/axiosConfig";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Provider store={store}>
      <App />
    </Provider>
  </React.StrictMode>
);

/*
خط 1
import React from "react";


در React نسخه‌های جدید لازم نیست همیشه React را import کنیم،
ولی خیلی از پروژه‌ها هنوز هم این کار را می‌کنند.

این خط React را وارد می‌کند.

بدون این، JSX در بعضی نسخه‌ها کار نمی‌کرد.

🔹 خط 2
import ReactDOM from "react-dom/client";


ReactDOM بخشی از React است که مسئول ارتباط با مرورگر است.

یعنی: «کد React » → «صفحه HTML»

react-dom/client نسخه جدیدی است که از React 18 معرفی شد.

🔹 خط 3
import App from "./App.jsx";


فایل اصلی برنامه را وارد می‌کنیم (کامپوننت بالادست کل برنامه).

تمام صفحه‌ها، روت‌ها، استورها… داخل App قرار می‌گیرند.

🔹 خط 4
import './index.css'


این فایل فقط برای استایل‌ها است.

Tailwind یا CSS معمولی می‌تواند داخل آن باشد.

🔹 خط 5
import { Provider } from "react-redux";

Provider چیه؟

خیلی ساده:

یک "جعبه/پوسته" است که Store را به کل برنامه وصل می‌کند.

هر کامپوننتی که داخل Provider باشد،
می‌تواند از useSelector و useDispatch استفاده کند.

بدون Provider، Redux هیچ معنایی ندارد.

🔹 خط 6
import { store } from "./store/store.js";


Store اصلی برنامه وارد می‌شود.

همان Store که authReducer داخلش بود.

🔹 خط 7
import "./api/axiosConfig";


این فایل معمولاً تنظیمات پیش‌فرض axios را انجام می‌دهد.

مثل:

آدرس سرور (baseURL)

interceptor برای اضافه کردن token

timeout ها

چون فقط import شده و ذخیره نشده، یعنی فقط اجرایش مهم است.

🟥 حالا مهم‌ترین بخش: رندر کردن برنامه

کد:

ReactDOM.createRoot(document.getElementById("root")).render(


این قسمت چند بخش دارد:

🔹 ReactDOM.createRoot(...)

createRoot یعنی:

«یه نقطه شروع برای برنامه React بساز.»

React 18 این روش را معرفی کرد (روش قبلی ReactDOM.render() بود).

🔹 document.getElementById("root")

این کد HTML صفحه اصلی است.

داخل فایل public/index.html، یک div هست:

<div id="root"></div>


React باید داخل این div اجرا شود.

پس این کد می‌گوید:

«برنامه را داخل این <div id="root"> وارد کن.»

🔹 render(...)

این تابع برنامه را روی صفحه نمایش می‌دهد.

و حالا می‌رسیم به بخش داخلی render.

🟩 داخل render

کد:

<React.StrictMode>
    <Provider store={store}>
        <App />
    </Provider>
</React.StrictMode>


بیای تمامش رو کامل توضیح بدیم:

🔷 1. <React.StrictMode>

ابزاری مخصوص حالت توسعه (development) است.

به اشتباه‌ها هشدار می‌دهد.

اگر کدی اشتباه باشد، یا deprecated باشد، console هشدار می‌دهد.

در نهایت در نسخه نهایی (production) حذف می‌شود.

🔷 2. <Provider store={store}>

این یکی از مهم‌ترین قسمت‌های Redux است.

Provider چه کار می‌کند؟

store داخل خودش نگه می‌دارد.

هر کامپوننتی داخل Provider باشد،
می‌تواند از Redux استفاده کند.

ساختار:

Provider
    App


یعنی App و تمام کامپوننت‌های داخلش،
می‌توانند:

useSelector

useDispatch

استفاده کنند.

🔷 3. <App />

و در نهایت:

برنامه اصلی اجرا می‌شود.

همه چیز از App شروع می‌شود.

🎉 جمع‌بندی ساده و مختصر

فایل index:

برنامه React را به مرورگر وصل می‌کند.

Store را با Provider به کل برنامه تزریق می‌کند.

فایل تنظیمات axios را اجرا می‌کند.

App را نمایش می‌دهد.

از StrictMode برای دیباگ بهتر استفاده می‌کند.
*/